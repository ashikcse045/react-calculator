import Calculator from './Calculator';

export default function Layout() {
    return (
        <main>
            <div className="container">
                <Calculator />
            </div>
        </main>
    );
}
